<body>
        <h1>Liste des utilisateurs</h1>

        <table>
                <tr>
                        <th>Login</th>
                        <th>Mot de passe</th>
                        <th>Nom</th>
                        <th>Prénom</th>
                        <th>Date de naissance</th>
                        <th>Statut</th>
                </tr>
                {% for user in utilisateurs %}
                        <tr>
                                <td>{{ user.getIdentifiant }}</td>
                                <td>{{ user.getMotdepasse() }}</td>
                                <td>{{ user.getNom() }}</td>
                                <td>{{ user.getPrenom() }}</td>
                                <td>{{ user.getAnniversaire()|date('d-m-Y') }}</td>
                                <td>{{ user.getIsadmin() }}</td>
                        </tr>
                {% endfor %}
        </table>
</body>